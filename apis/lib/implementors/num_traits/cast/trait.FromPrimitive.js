(function() {var implementors = {};
implementors["braid_server"] = ["impl FromPrimitive for <a class='enum' href='https://docs.rs/chrono/0.3.0/chrono/enum.Weekday.html' title='chrono::Weekday'>Weekday</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
